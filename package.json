{
  "name": "sightline",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html app.js styles.css dist/",
    "dev": "python3 -m http.server 4173",
    "start": "python3 -m http.server 4173"
  }
}
